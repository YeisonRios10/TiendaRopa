# Impedir que se pueda ver el listado de contenidos de un directorio

Options All -Indexes

# Url Amigables
# modulo que permite reescribir la info que se envia por url desde un vinculo
RewriteEngine On

# regla de reescritura con expresion regular
RewriteRule ^([-a-zA-Z0-9ñÑ_/]+)$ index.php?ruta=$1